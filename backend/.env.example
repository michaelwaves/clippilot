# MiniMax Hosted API Configuration
MINIMAX_API_KEY=
MINIMAX_MCP_BASE_PATH=
MINIMAX_API_HOST=https://api.minimax.io
MINIMAX_API_RESOURCE_MODE=url

# Optional: OpenAI API key for fallback LLM
OPENAI_API_KEY=

# Note: You'll need to set OPENAI_API_KEY for the LlamaIndex agent to work
# The agent uses OpenAI's models as the LLM while calling MiniMax tools via MCP